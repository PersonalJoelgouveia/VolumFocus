/** Abas internas do hub Saúde (Sidebar > Saúde). Só navegação — sem
 *  relação com `ViewId` (Saúde continua sendo uma única view no shell). */
export type SaudeTab = 'visao-geral' | 'avaliacao' | 'exames' | 'wearables';
